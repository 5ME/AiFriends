# 部署方案对比

> 前提假设：**服务器已装好 Docker + Docker Compose v2**，且服务器为 `linux/amd64`。
> 三套方案都基于同一套「Docker Compose 5 容器」架构（PostgreSQL + Redis + Django/gunicorn + Celery + Nginx），
> 区别只在于：**镜像在哪 build、怎么到服务器、前端怎么进镜像**。

> ⚠️ **内存约束（2 vCPU / 2GB 阿里云 ECS）**：2GB 跑 5 个容器**运行没问题**，但**跑 `npm install` + vite build 容易 OOM**（这正是当初 D1 把前端 build 挪到本地的原因）。因此「在服务器上 build 镜像」这条路对 2GB **不可行**，镜像必须在**本地或 CI** 上 build——save/load 和 registry 都满足这一点。

---

## 0. 一句话概括

| 方案 | 核心思路 | 服务器端动作 |
|------|---------|-------------|
| **现行方案** | 前端本地 build + scp 静态文件；后端镜像在服务器上 build | build 后端 + 挂静态文件 |
| **save/load 方案** | 前后端在本地 build 成完整镜像，导出 tar 传上去 | `load` 后直接 `up` |
| **registry 方案** | 完整镜像推到中央仓库，服务器拉取 | `pull` 后直接 `up` |

---

## 1. 现行方案（现状）

### 流程图

```
本地构建机                          服务器
─────────                          ─────
npm run build (前端)      ──scp──▶  staticfiles/（bind mount）
collectstatic             ──scp──▶  media/（default.png）
                                      │
                                      │ docker compose build（现场 build django/celery 镜像）
                                      ▼
                                   docker compose up -d
```

### 全新部署步骤（约 13 步，跨两台机器）

```bash
# 服务器
git clone ... && git checkout <分支>          # 拉源码
cp .env.example .env && vim .env             # 配密钥
mkdir ssl && openssl req ...                 # 生成自签证书
# 本地
cd frontend && npm install
$env:VITE_PLATFORM='docker'; npm run build   # ⚠️ 模式错了全盘指向 127.0.0.1
cd ../backend && python manage.py collectstatic --noinput
scp -r backend/staticfiles <服务器>:backend/  # 传前端产物
scp backend/media/user/photos/default.png <服务器>  # ⚠️ 文档没写，不传就 404
# 服务器
sudo chown -R gqyin:gqyin backend/{staticfiles,media,logs}  # ⚠️ 文档没写，否则 scp 权限拒绝
docker compose run --rm django python manage.py migrate
docker compose up -d --build                 # 现场 build 后端镜像（几分钟）
docker compose ps
```

### 优缺点

**优点**
- 无需外置仓库、无需额外服务，最「原始」也最直白
- 后端镜像 build 用到的 apt/pip 已走国内源

**缺点**
- 跨两台机器，13+ 手工步骤，出错点多
- 前端 build 结果靠 `scp`，无任何校验（`VITE_PLATFORM` 错一次就废）
- `default.png`、bind mount 属主、路径不一致这些坑文档都没写，每次新环境重踩
- 后端镜像在服务器上现 build，依赖服务器网络 + 时间

---

## 2. save/load 方案（本地打包、传镜像）

### 流程图

```
本地构建机                          服务器
─────────                          ─────
docker build（多阶段：含前端）       ──scp──▶  aifriends-image.tar.gz
docker save | gzip                 ──scp──▶  docker-compose.yml / .env / ssl / media
                                      │
                                      │ docker load -i aifriends-image.tar.gz
                                      ▼
                                   docker compose up -d
```

### 全新部署步骤（约 6 步）

```bash
# 本地（一次打包好所有东西）
docker build -t aifriends:latest -f backend/Dockerfile .   # 多阶段，前端打进镜像
docker save aifriends:latest | gzip > aifriends-image.tar.gz
scp aifriends-image.tar.gz docker-compose.yml nginx.conf gqyin@<IP>:~/app/
scp .env gqyin@<IP>:~/app/                     # 密钥单独传（ssl 证书在服务器上生成，见第 5 节）
scp -r backend/media gqyin@<IP>:~/app/media   # 仅当要带用户数据

# 服务器
cd ~/app
docker load -i aifriends-image.tar.gz   # 导入镜像
docker compose up -d                     # 直接启动，不 build
```

### 优缺点

**优点**
- 服务器只 `load` + `up`，零 build、零 npm、零拉后端镜像
- 无外置仓库依赖，最接近「本地做好、传上去直接启动」
- 前端 build 一次性固化进镜像，`VITE_PLATFORM=docker` 在本地 build 期就定死，不会错

**缺点**
- 每次部署都传**全量镜像 tar（约 1~2GB）**，改前端也一样要传全量
- 需要本地装 Docker 并会用它 build（对纯前端/后端的本地机有要求）
- `docker-compose.yml` 要改成 `image:` 引用，不能再 `build:`

---

## 3. registry 方案（中央镜像仓库）

### 流程图

```
本地 或 CI（GitHub Actions）
─────────
docker build（多阶段）
docker push  →  镜像仓库（阿里云 ACR）
                    │
                    │ docker compose pull（增量，只拉变更层）
                    ▼
                服务器：docker compose pull && up -d
```

### 全新部署步骤（约 5 步）

```bash
# 一次性：本地/CI 推镜像到仓库
docker build -t <仓库>/aifriends:latest .
docker push <仓库>/aifriends:latest

# 服务器（首次）
docker login <仓库> -u <user> -p <token>   # 一次性凭证
docker compose pull
docker compose up -d
```

> 若走 CI：push 代码 → GitHub Actions 自动 build + push + SSH 触发服务器 pull，即「push 即部署」。

### 优缺点

**优点**
- 服务器永远只「拉 + 起」，两个动作，零 build
- **增量传输**：改前端只拉几 MB 差异层，不传 1~2GB
- 本地/CI 与服务器解耦，不需直接 scp 到服务器
- 天然通向「push 代码自动上线」

**缺点**
- 多一套仓库账号 + 服务器 `docker login` 凭证
- 走 CI 还要配 GitHub secrets（仓库凭证、服务器 SSH key）
- **国内必须选腾讯云 TCR / 阿里云 ACR**，GitHub GHCR / Docker Hub 都慢
- 比 save/load 多一个「外置仓库」依赖

### 仓库选择（国内）

| 仓库 | 国内速度 | 费用 | 备注 |
|------|---------|------|------|
| **阿里云 ACR** | 快 | 个人版免费（额度待核实） | ⭐ 同区域用 `-vpc` 内网域名拉取，见下方说明 |
| 腾讯云 TCR | 快 | 有免费额度 | 需注册腾讯云 |
| GitHub GHCR | 慢/不稳 | 免费 | 国内访问不稳，不推荐当生产仓库 |
| Docker Hub | 慢 + 限流 | 免费 | 需配镜像加速器 |

> ⚠️ **ACR 个人版：地域与内网（2024-09 新规则，基于阿里云官方公告）**
> - **地域绑定**：新个人版创建时须选一个地域，之后只能在该地域使用。
> - **域名**：公网 `crpi-xxxx.<region>.personal.cr.aliyuncs.com`；内网 `crpi-xxxx-vpc.<region>.personal.cr.aliyuncs.com`（`-vpc` 仅同地域 VPC 可访问）。
> - **同区域才走内网**：ECS 与 ACR 建在同一地域（如都华东2上海）→ 用 `-vpc` 域名，免公网流量费 + 低延迟；跨区域只能用公网域名。
> - **共享带宽 + 限流**：无固定出口带宽保障，并发拉取 >10 或频繁拉取会触发 `TOOMANYREQUESTS`。个人项目偶尔拉取够用，不必上企业版。

> 📌 **ACR 关键事实核实记录（以阿里云官方最新文档为准）**
> 1. ✅ **已核实 — 个人版免费额度**：个人版**公测限额免费使用，拉取和上传镜像均免费**，但**无 SLA 承诺**。命名空间限额 **3 个**、公开/私有仓库限额各 **300 个**。
> 2. ✅ **已核实 — 流量费**：个人版拉取/上传均免费，**无单独流量费**（区别于企业版需自担 OSS 外网流出流量费）。走 `-vpc` 内网免公网流量、低延迟；走公网域名则速度/延迟变差。
> 3. ✅ **已核实 — `-vpc` 内网访问**：个人版 ACR **默认支持** `-vpc` 域名从**同地域** ECS 走内网拉取，**无需** PrivateZone 或显式绑定 VPC（企业版才需要）。前提：ECS 在 VPC 网络 + 同地域 + 用对 `-vpc` 域名。内网域名格式以官方公告 `crpi-xxxx-vpc.{region}.personal.cr.aliyuncs.com` 为准（AI 助理给的 `*-registry-vpc.{region}.cr.aliyuncs.com` 疑似旧版格式），以控制台实际显示为准。
> 4. ✅ **已核实 — 认证方式**：规格表「免密拉取」对 **2024-09-04 之后创建的实例 = ❌（不支持）**。故新个人版实例需用 `docker login`（RAM 子账号 + 设置登录密码）认证。
> 5. ⏸️ **企业版价格（低优先级）**：仅升级企业版才需要。已见收费项（实例包年包月按规格 + OSS 按量 + 安全扫描 800元/月 + 命名空间/仓库/专有网络增量配额），具体实例价格以购买页为准。当前个人版免费够用，暂不深究。

---

## 4. 三方案对比总表

| 维度 | 现行方案 | save/load | registry |
|------|---------|-----------|----------|
| 服务器端动作 | build 后端 + 挂静态 | `load` + `up` | `pull` + `up` |
| 每次传输量 | 前端产物（几 MB） | 全量镜像 1~2GB | 增量（几 MB~几十 MB） |
| 需要外置仓库 | ❌ | ❌ | ✅ |
| 需要本地 Docker | ❌（只需 node + py） | ✅ | ✅（或走 CI） |
| 能否「push 即部署」 | ❌ | ❌ | ✅ |
| 前端模式出错风险 | 高（手工 scp 无校验） | 低（build 期固化） | 低（build 期固化） |
| 配置复杂度 | 中（坑多） | 低 | 中（仓库 + secrets） |
| 全新部署步骤数 | ~13 | ~6 | ~5 |
| 适合场景 | 现状 | 偶尔部署、想零外置依赖 | 频繁部署、想自动化 |

---

## 5. 三方案都绕不开的共性问题（与「怎么传镜像」无关）

无论选哪套，下面这几件事都要单独处理，别指望镜像方案自动解决：

1. **默认头像 `default.png`**：现在是 gitignore 的 media 文件，全新环境必 404。正确做法是**挪到 `/static/`**（随镜像/静态产物走），或部署时 seed。→ 这属于「多阶段 Dockerfile」改造的一部分。
2. **`.env`（密钥）**：绝不能打进镜像，每次都要单独传/配。这是安全要求，不是负担。
3. **SSL 证书**：`ssl/` 目录**在服务器上**用 openssl 生成（自签证书，含 `CN=服务器IP`），本地本来就没有、也不需要传；有域名则建议改用 Let's Encrypt 免费用正式证书（无浏览器警告）。
4. **media（用户上传数据）**：bind mount 持久化，全新部署时要么空着、要么迁移老数据。
5. **镜像架构**：本地和服务器必须都是 `amd64`（你目前 Windows x86 → 云主机 x86，一致；换 M 芯片 Mac 需 `--platform linux/amd64`）。
6. **bind mount 属主**：`staticfiles/media/logs` 被 Docker 以 root 建目录后，非 root 用户写不进 → scp 权限拒绝。需 `chown` 或容器改用非 root 用户。

---

## 6. 决策建议

- **想最小改动、又能接受「偶尔传一次全量 tar」** → 选 **save/load**：零外置依赖，最贴近你说的「本地做好、传上去直接启动」。
- **想「push 代码自动上线」、或以后会频繁改前端** → 选 **registry（阿里云 ACR）**：增量传输 + 可接 CI。
- **两者其实共用同一个前置改造**：**多阶段 Dockerfile（前端打进镜像）**。这是关键一步，做了它，「前端 scp」和「服务器 build 后端」这两个最大的手工环节就同时消失了。

> 无论选哪个，都建议顺手把第 5 节里的共性问题一起解决（尤其 default.png 进 static、bind mount 属主），否则换套方案仍会重踩这次踩过的坑。

---

*生成日期：2026-08-18。本文为决策对比文档，选定方向后再产出正式设计（spec）+ 实施计划（plan）。*
