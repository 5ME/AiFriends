# P0 Fixes — 正确性 & 安全修复

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 3 个 P0 正确性/安全 Bug + README 同步更新。

**Architecture:** 小范围精准修改 — 2 个文件修查询值、3 个文件修异常处理、1 个文件提取配置到环境变量、1 个文件更新文档。每个修改独立可验证。

**Tech Stack:** Django 6.0, pytest, PostgreSQL 17 + pgvector

**Branch:** `feature/gqyin/p0-fixes`

---

## File Map

| 文件 | 操作 | 负责 |
|------|------|------|
| `backend/web/views/friend/message/chat/chat.py:52` | Modify | P0-1: 修复 SystemPrompt 查询值 |
| `backend/web/views/friend/message/memory/update.py:13` | Modify | P0-1: 修复 SystemPrompt 查询值 |
| `backend/web/tests/test_system_prompt.py` | Create | P0-1: 新增 SystemPrompt 加载测试 |
| `backend/backend/settings.py:23-28` | Modify | P0-2: 环境变量化 |
| `backend/.env.example` | Modify | P0-2: 补充 Django 配置变量 |
| `backend/.env` | Modify | P0-2: 配置实际值 |
| `backend/web/views/create/character/get_single.py:19` | Modify | P0-3: DoesNotExist → 404 |
| `backend/web/views/create/character/update.py:21` | Modify | P0-3: DoesNotExist → 404 |
| `backend/web/views/create/character/remove.py:20` | Modify | P0-3: DoesNotExist → 404 |
| `backend/web/tests/test_character.py:111,147,168` | Modify | P0-3: 断言 500 → 404 |
| `README.md` | Modify | P0-4: 同步当前技术栈 |

---

### Task 1: P0-1 — 修复 SystemPrompt.title 查询值

**Files:**
- Modify: `backend/web/views/friend/message/chat/chat.py:52`
- Modify: `backend/web/views/friend/message/memory/update.py:13`
- Create: `backend/web/tests/test_system_prompt.py`

- [ ] **Step 1: 编写失败的测试 — add_system_prompt 应加载 SystemPrompt 记录**

```python
# backend/web/tests/test_system_prompt.py
import pytest
from django.utils.timezone import now

from web.models.friend import SystemPrompt, Friend
from langchain_core.messages import HumanMessage


class TestSystemPromptLoading:
    """验证 SystemPrompt 查询使用正确的枚举值"""

    def test_add_system_prompt_loads_reply_prompts(self, friend, user_profile, character):
        """chat.py 的 add_system_prompt 应加载 title='reply' 的 SystemPrompt"""
        from web.views.friend.message.chat.chat import add_system_prompt

        # 创建一条 reply 类型的 SystemPrompt
        SystemPrompt.objects.create(
            title=SystemPrompt.Title.REPLY,
            order_number=0,
            prompt="【测试规则】请用文言文回答",
        )

        inputs = {"messages": [HumanMessage(content="你好")]}
        result = add_system_prompt(inputs, friend)

        messages = result["messages"]
        assert len(messages) == 2  # SystemMessage + HumanMessage
        system_msg = messages[0]
        assert "请用文言文回答" in system_msg.content
        assert friend.character.system_prompt in system_msg.content

    def test_create_system_message_loads_memory_prompts(self, friend):
        """memory/update.py 的 create_system_message 应加载 title='memory' 的 SystemPrompt"""
        from web.views.friend.message.memory.update import create_system_message

        SystemPrompt.objects.create(
            title=SystemPrompt.Title.MEMORY,
            order_number=0,
            prompt="【记忆规则】请用第三人称总结",
        )

        msg = create_system_message()
        assert "请用第三人称总结" in msg.content
```

- [ ] **Step 2: 运行测试验证失败**

```bash
python -m pytest web/tests/test_system_prompt.py -v
```

预期：**FAIL** — `assert "请用文言文回答" in system_msg.content` 断言失败，因为 `title__exact='回复'` 查不到 `title='reply'` 的记录。

- [ ] **Step 3: 修复 chat.py — 使用枚举值查询**

在 `backend/web/views/friend/message/chat/chat.py:52`，修改：

```python
# Before
system_prompts = SystemPrompt.objects.filter(title__exact='回复').order_by('order_number')
# After
system_prompts = SystemPrompt.objects.filter(title=SystemPrompt.Title.REPLY).order_by('order_number')
```

- [ ] **Step 4: 修复 memory/update.py — 使用枚举值查询**

在 `backend/web/views/friend/message/memory/update.py:13`，修改：

```python
# Before
system_prompts = SystemPrompt.objects.filter(title__exact='记忆').order_by('order_number')
# After
system_prompts = SystemPrompt.objects.filter(title=SystemPrompt.Title.MEMORY).order_by('order_number')
```

- [ ] **Step 5: 运行测试验证通过**

```bash
python -m pytest web/tests/test_system_prompt.py -v
```

预期：**2 passed** — 两条测试均通过。

- [ ] **Step 6: 运行全量测试确保无回归**

```bash
python -m pytest web/tests/ -v
```

预期：**51 passed**（原 49 + 新增 2）。

- [ ] **Step 7: Commit**

```bash
git add backend/web/views/friend/message/chat/chat.py backend/web/views/friend/message/memory/update.py backend/web/tests/test_system_prompt.py
git commit -m "fix: use TextChoices enum values for SystemPrompt queries

SystemPrompt.title uses TextChoices with DB values 'reply'/'memory',
but queries used Chinese labels ('回复'/'记忆'), causing SystemPrompt
records to never be loaded for Chat Agent and Memory Agent.

- chat.py: title__exact='回复' → title=SystemPrompt.Title.REPLY
- memory/update.py: title__exact='记忆' → title=SystemPrompt.Title.MEMORY
- Add test_system_prompt.py to verify prompt loading

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 2: P0-2 — SECRET_KEY / DEBUG / ALLOWED_HOSTS 环境变量化

**Files:**
- Modify: `backend/backend/settings.py:23-28`
- Modify: `backend/.env.example`
- Modify: `backend/.env`

- [ ] **Step 1: 修改 settings.py — 从环境变量读取关键配置**

在 `backend/backend/settings.py`，将第 23-28 行：

```python
# Before
SECRET_KEY = 'django-insecure-0xy8(za45g9-an!z&4u*lf@g38j24g!2+&6o^faq$%#n046mwz'

DEBUG = True

ALLOWED_HOSTS = ['127.0.0.1', '115.190.245.146']
```

替换为：

```python
# After
SECRET_KEY = os.environ.get('DJANGO_SECRET_KEY', 'django-insecure-change-me-in-production')

DEBUG = os.environ.get('DJANGO_DEBUG', 'False').lower() == 'true'

ALLOWED_HOSTS = os.environ.get('DJANGO_ALLOWED_HOSTS', '127.0.0.1').split(',')
```

- [ ] **Step 2: 更新 .env.example — 补充 Django 配置变量**

在 `backend/.env.example` 末尾追加：

```ini
# Django 核心配置
DJANGO_SECRET_KEY=
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
```

- [ ] **Step 3: 更新 .env — 配置本地开发值**

在 `backend/.env` 中追加（SECRET_KEY 需生成新值）：

```ini
# Django 核心配置
DJANGO_SECRET_KEY=django-insecure-local-dev-key-do-not-use-in-prod
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
```

- [ ] **Step 4: 验证 Django 能正常启动**

```bash
cd backend && python manage.py check
```

预期：**System check identified no issues (0 silenced).**

- [ ] **Step 5: 运行全量测试确保无回归**

```bash
cd backend && python -m pytest web/tests/ -v
```

预期：**51 passed**。

- [ ] **Step 6: Commit**

```bash
git add backend/backend/settings.py backend/.env.example
git commit -m "fix: move SECRET_KEY, DEBUG, ALLOWED_HOSTS to environment variables

Previously hardcoded in settings.py — SECRET_KEY exposed in git history,
DEBUG always True, ALLOWED_HOSTS contained production IP.

Now read from DJANGO_SECRET_KEY, DJANGO_DEBUG, DJANGO_ALLOWED_HOSTS
environment variables with safe defaults.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

**注意：** `.env` 在 `.gitignore` 中，不会被提交。需要在部署环境手动配置。

---

### Task 3: P0-3 — 修复非作者访问角色返回 500

**Files:**
- Modify: `backend/web/views/create/character/get_single.py:18-20`
- Modify: `backend/web/views/create/character/update.py:20-22`
- Modify: `backend/web/views/create/character/remove.py:19-21`
- Modify: `backend/web/tests/test_character.py:105-111, 135-147, 162-168`

- [ ] **Step 1: 修复 get_single.py — DoesNotExist → 404**

在 `backend/web/views/create/character/get_single.py`，将第 18-44 行的整个 `try` 块重构：

```python
# Before (lines 17-44)
def get(self, request):
    try:
        character_id = request.query_params.get('character_id')
        character = Character.objects.get(id=character_id, author__user=request.user)
        voices_raw = Voice.objects.order_by('id')
        voices = []
        for voice in voices_raw:
            voices.append({
                'id': voice.id,
                'name': voice.name,
                'profile': voice.profile,
            })
        return Response({
            'message': 'success',
            'character': {
                'id': character.id,
                'name': character.name,
                'introduction': character.introduction,
                'system_prompt': character.system_prompt,
                'photo': character.photo_url,
                'background_image': character.background_image_url,
                'voice_id': character.voice.id,
            },
            'voices': voices,
        })
    except Exception as e:
        logger.exception('获取角色详情异常: %s', e)
        return Response({'message': '系统异常'},
                        status=status.HTTP_500_INTERNAL_SERVER_ERROR)
```

替换为：

```python
# After
def get(self, request):
    try:
        character_id = request.query_params.get('character_id')
        try:
            character = Character.objects.get(id=character_id, author__user=request.user)
        except Character.DoesNotExist:
            return Response({'message': '角色不存在或无权访问'},
                            status=status.HTTP_404_NOT_FOUND)
        voices_raw = Voice.objects.order_by('id')
        voices = []
        for voice in voices_raw:
            voices.append({
                'id': voice.id,
                'name': voice.name,
                'profile': voice.profile,
            })
        return Response({
            'message': 'success',
            'character': {
                'id': character.id,
                'name': character.name,
                'introduction': character.introduction,
                'system_prompt': character.system_prompt,
                'photo': character.photo_url,
                'background_image': character.background_image_url,
                'voice_id': character.voice.id,
            },
            'voices': voices,
        })
    except Exception as e:
        logger.exception('获取角色详情异常: %s', e)
        return Response({'message': '系统异常'},
                        status=status.HTTP_500_INTERNAL_SERVER_ERROR)
```

- [ ] **Step 2: 修复 update.py — DoesNotExist → 404**

在 `backend/web/views/create/character/update.py`，将第 20-22 行的 `Character.objects.get()` 包裹 `try/except`：

```python
# Before (lines 18-22)
def post(self, request):
    try:
        character_id = request.data['character_id']
        character = Character.objects.get(id=character_id, author__user=request.user)
        name = request.data['name'].strip()
```

替换为：

```python
# After
def post(self, request):
    try:
        character_id = request.data['character_id']
        try:
            character = Character.objects.get(id=character_id, author__user=request.user)
        except Character.DoesNotExist:
            return Response({'message': '角色不存在或无权访问'},
                            status=status.HTTP_404_NOT_FOUND)
        name = request.data['name'].strip()
```

- [ ] **Step 3: 修复 remove.py — DoesNotExist → 404**

在 `backend/web/views/create/character/remove.py`，将第 19-21 行：

```python
# Before (lines 17-21)
def post(self, request):
    try:
        character_id = request.data['character_id']
        character = Character.objects.get(id=character_id, author__user=request.user)
        remove_old_photo(character.photo)
```

替换为：

```python
# After
def post(self, request):
    try:
        character_id = request.data['character_id']
        try:
            character = Character.objects.get(id=character_id, author__user=request.user)
        except Character.DoesNotExist:
            return Response({'message': '角色不存在或无权访问'},
                            status=status.HTTP_404_NOT_FOUND)
        remove_old_photo(character.photo)
```

- [ ] **Step 4: 更新测试断言 — 500 → 404**

在 `backend/web/tests/test_character.py`，修改 3 处断言：

**Line 105-111 (`test_get_single_other_author`):**
```python
# Before
def test_get_single_other_author(self, other_auth_client, character):
    """查看别人的 → 500（按 author 过滤找不到）"""
    resp = other_auth_client.get(
        "/api/create/character/get_single/",
        {"character_id": character.id},
    )
    assert resp.status_code == status.HTTP_500_INTERNAL_SERVER_ERROR
```
```python
# After
def test_get_single_other_author(self, other_auth_client, character):
    """查看别人的 → 404"""
    resp = other_auth_client.get(
        "/api/create/character/get_single/",
        {"character_id": character.id},
    )
    assert resp.status_code == status.HTTP_404_NOT_FOUND
```

**Line 135-147 (`test_update_not_author`):**
```python
# Before
def test_update_not_author(self, other_auth_client, character, voice):
    """非作者编辑 → 500（按 author 过滤找不到）"""
    ...
    assert resp.status_code == status.HTTP_500_INTERNAL_SERVER_ERROR
```
```python
# After
def test_update_not_author(self, other_auth_client, character, voice):
    """非作者编辑 → 404"""
    ...
    assert resp.status_code == status.HTTP_404_NOT_FOUND
```

**Line 162-168 (`test_delete_not_author`):**
```python
# Before
def test_delete_not_author(self, other_auth_client, character):
    """非作者删除 → 500（按 author 过滤找不到）"""
    ...
    assert resp.status_code == status.HTTP_500_INTERNAL_SERVER_ERROR
```
```python
# After
def test_delete_not_author(self, other_auth_client, character):
    """非作者删除 → 404"""
    ...
    assert resp.status_code == status.HTTP_404_NOT_FOUND
```

- [ ] **Step 5: 运行全量测试验证**

```bash
python -m pytest web/tests/ -v
```

预期：**51 passed**（含 Task 1 新增的 2 个测试）。

- [ ] **Step 6: 确认真异常仍返回 500**

测试仍覆盖真实的 500 场景：`test_get_single.py` 的外层 `except Exception` 在 DB 连接失败等真实故障时仍返回 500。

- [ ] **Step 7: Commit**

```bash
git add backend/web/views/create/character/get_single.py backend/web/views/create/character/update.py backend/web/views/create/character/remove.py backend/web/tests/test_character.py
git commit -m "fix: return 404 instead of 500 for unauthorized character access

Previously Character.objects.get() with author filter raised DoesNotExist
for non-author users, caught by broad except Exception → 500. Now explicitly
catches DoesNotExist and returns 404 with a clear error message.

Also update test assertions from 500 to 404 for these scenarios.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 4: P0-4 — README 同步更新

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 更新技术栈表格（Lines 7-15）**

```markdown
| 层级 | 技术 |
|------|------|
| 后端 | Django 6.0 + Django REST Framework + JWT (PostgreSQL 17 + pgvector 0.8) |
| 前端 | Vue 3 (Composition API) + Vite 7 + Pinia + Vue Router 5 |
| UI | Tailwind CSS 4 + daisyUI 5 |
| AI | 阿里云 DashScope（通义千问 / DeepSeek-V3.2） + LangChain/LangGraph |
| 语音 | DashScope TTS (WebSocket) + ASR (WebSocket) + 浏览器端 VAD (Silero VAD / ONNX) |
| 向量存储 | pgvector (1024 维 DashScope text-embedding-v4) |
| 部署 | Gunicorn + Nginx (Ubuntu) |
```

- [ ] **Step 2: 更新功能列表（Lines 19-24）**

```markdown
- 创建 / 编辑 / 删除自定义 AI 虚拟角色
- 文字 + 语音双模态聊天，AI 回复支持文字 + 语音同步流式输出
- 聊天上下文长期记忆，每 10 条消息触发 Memory Agent 自动摘要
- RAG 知识库检索（pgvector 余弦距离搜索）
- 用户注册登录（JWT access/refresh 双令牌认证，httpOnly cookie）
- 首页角色探索 + 好友关系管理
- 49 个 pytest 自动化测试覆盖核心链路
```

- [ ] **Step 3: 更新快速开始 — 补充环境配置和测试**

```markdown
### 后端

```bash
cd backend
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env 填入 API_KEY 等必填项

# 2. 安装依赖
pip install -r requirements.txt

# 3. 数据库迁移
python manage.py migrate

# 4. 启动开发服务器
python manage.py runserver        # http://127.0.0.1:8000
```

### 运行测试

```bash
cd backend
python -m pytest web/tests/ -v   # 51 个测试
```
```

- [ ] **Step 4: 更新项目结构 — 补充 models/document.py + 删除 LanceDB 引用**

在项目结构树中，`models/` 部分增加：

```
│   │   ├── models/                       # 数据模型
│   │   │   ├── user.py                   #   UserProfile
│   │   │   ├── character.py              #   Character、Voice
│   │   │   ├── friend.py                 #   Friend、Message、SystemPrompt
│   │   │   └── document.py               #   DocumentChunk (pgvector 向量字段)
```

- [ ] **Step 5: 新增 Known Limitations 章节**

在 README 末尾新增：

```markdown
## 已知限制

- 知识库目前为全局预置，暂不支持用户自行上传文档构建个人 RAG
- 音色仅支持系统内置，暂不支持用户自定义
- Memory Agent 当前在聊天请求线程内同步执行，大模型调用期间会占用 worker 资源
- 未做 Docker 容器化，部署需手动配置环境
- 未做压测，暂无容量评估数据
- 测试环境使用 SQLite，pgvector 查询仅在 PostgreSQL 运行环境中验证
```

- [ ] **Step 6: Commit**

```bash
git add README.md
git commit -m "docs: sync README with current PostgreSQL/pgvector/pytest stack

Update tech stack, features, project structure, quick start, and add
Known Limitations section to reflect post-migration state.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Verification Checklist

完成所有 Task 后，逐项确认：

```
[ ] P0-1: add_system_prompt() 能加载 reply 类型的 SystemPrompt 记录
[ ] P0-1: create_system_message() 能加载 memory 类型的 SystemPrompt 记录
[ ] P0-1: 全量 51 个测试通过
[ ] P0-2: SECRET_KEY 从环境变量 DJANGO_SECRET_KEY 读取
[ ] P0-2: DEBUG 从环境变量 DJANGO_DEBUG 读取，默认 False
[ ] P0-2: ALLOWED_HOSTS 从环境变量 DJANGO_ALLOWED_HOSTS 读取
[ ] P0-2: .env.example 包含新增的 Django 配置变量
[ ] P0-2: python manage.py check 无报错
[ ] P0-3: 非作者查看角色返回 404（非 500）
[ ] P0-3: 非作者编辑角色返回 404（非 500）
[ ] P0-3: 非作者删除角色返回 404（非 500）
[ ] P0-3: 真实异常（DB 故障等）仍返回 500
[ ] P0-4: README 技术栈表写 PostgreSQL/pgvector/pytest
[ ] P0-4: README 功能列表与代码一致
[ ] P0-4: README 有测试运行命令
[ ] P0-4: README 有 Known Limitations
```

---

## PR 提交

```bash
# 分支已创建: feature/gqyin/p0-fixes
# 包含 4 个 commits

gh pr create --title "fix: P0 correctness and security fixes" --body "$(cat <<'EOF'
## Summary
- Fix SystemPrompt.title queries using Chinese labels instead of TextChoices enum values (Chat Agent + Memory Agent system prompts were never loaded)
- Move SECRET_KEY, DEBUG, ALLOWED_HOSTS to environment variables
- Return 404 instead of 500 for unauthorized character access
- Sync README with current PostgreSQL/pgvector/pytest stack

## Test
- 51 pytest tests pass (49 existing + 2 new SystemPrompt loading tests)
- `python manage.py check` passes after config changes

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```
