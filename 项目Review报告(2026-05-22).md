# AI Friends 项目深度 Review（2026-05-22）

> 视角：资深后端架构师 / AI 应用技术面试官
> 目标岗位：Java 后端开发工程师 / AI 应用工程师（社招）
> 评估人背景假设：Java 为主语言，Python 基础，上一份工作后端开发

---

## 一、项目亮点（简历价值排序）

### S 级（必须写进简历，面试官最可能追问）

| 亮点 | 简历价值 | 理由 |
|------|---------|------|
| **双 Agent 架构（LangGraph）** | ⭐⭐⭐⭐⭐ | Chat Agent（deepseek-v3.2 + 工具调用）+ Memory Agent（自动摘要），这是 AI 应用的核心能力。面试官会追问：为什么用 LangGraph 而不是 LangChain Chain？State Graph 的状态管理怎么做的？工具调用（get_time / search_knowledge_base）的可靠性如何保证？ |
| **全双工流式语音对话** | ⭐⭐⭐⭐⭐ | VAD → ASR → LLM → TTS → MSE 播放，端到端实时流式。涉及 WebSocket 双向通信、SSE 流式传输、浏览器 MediaSource API、异步并发（asyncio.gather）。这是大多数后端工程师做不到的。 |
| **JWT 双 Token 无感刷新** | ⭐⭐⭐⭐ | access_token（2h）+ refresh_token（7d, httpOnly cookie），401 拦截器 + 订阅者队列（subscriber pattern）实现并发请求排队重试。这是真实生产级实现。 |
| **pgvector 向量检索 + RAG** | ⭐⭐⭐⭐ | 自实现 embedding 封装（DashScope text-embedding-v4），Markdown 分块策略（MarkdownHeaderTextSplitter + RecursiveCharacterTextSplitter），pgvector 余弦距离检索。 |

### A 级（加分项，选择性写进简历）

| 亮点 | 简历价值 | 理由 |
|------|---------|------|
| **Django REST Framework 纯 APIView 设计** | ⭐⭐⭐ | 每个端点一个文件，不使用 DRF Serializer，直接读取 request.data。体现对 DRF 的深入理解和自主设计能力。 |
| **前后端一体化部署** | ⭐⭐⭐ | Nginx + Gunicorn + Unix Socket 部署，HTTPS 自签名证书，Django 服务 SPA。 |
| **N+1 查询优化** | ⭐⭐⭐ | select_related 预加载、复合索引设计，对 ORM 性能有意识。 |
| **Dual-DB 策略（测试 SQLite / 生产 PG）** | ⭐⭐⭐ | 通过 sys.argv 自动检测 pytest，切换数据库引擎。 |

---

## 二、工程能力与架构能力评估

### 做得好的地方

**1. 项目结构清晰，关注点分离到位**

`web/views/` 下每个 API 端点一个文件，`models/` 按领域拆分（user / character / friend / document），前端组件按功能目录组织。对于个人项目来说，这已经是工程化意识的体现。

**2. 错误处理链路相对完整**

从最早 Review 暴露的 20+ 处裸 `except:` 到现在**零裸 except**，所有异常都有 `logger.exception()` 记录。chat.py 的后台线程异常也会通过 queue 传递错误事件到 SSE 流。这是一个显著的工程能力进步。

**3. SSE 流式架构设计合理**

- 生产者-消费者模式（queue.Queue）解耦线程
- 后台线程跑 asyncio event loop（TTS WebSocket）
- 主线程 yield SSE 事件
- 流结束后的 DB 写入在生成器内部完成（避免线程安全问题）

**4. 前端 Token 刷新架构是生产级实现**

`api.js` 的订阅者队列模式（`refreshSubscribers`）处理并发 401 重试，`streamApi.js` 的 SSE 重连机制处理流式连接的 Token 过期。这比大多数教程级项目强一个档次。

**5. SSE + MediaSource 音频流播放**

前端使用 `MediaSource` API 接收 SSE 中的 base64 音频块，通过 SourceBuffer 排队播放。使用了 `processId` 实现输出中断（用户发新消息时丢弃旧响应）。这是全栈能力的体现。

### 做得不够好的地方

**1. Django 视图中运行 asyncio 的线程安全问题**

```python
# chat.py line 158
thread = threading.Thread(target=self.work, args=(app, inputs, mq, voice_id))
thread.start()

# line 214
def work(self, ...):
    asyncio.run(self.run_tts_task(...))
```

这是**高风险设计**。Django 的同步视图在 WSGI 模式下，每个请求由独立的线程/进程处理。在同步视图中用 `threading.Thread` + `asyncio.run()` 启动异步任务，存在以下问题：
- Django 的数据库连接不是线程安全的（虽然你用了 `CONN_HEALTH_CHECKS=True`，但连接本身可能被复用）
- gunicorn worker 退出时，后台线程会被强杀，可能导致 TTS WebSocket 连接泄漏
- 错误恢复机制弱 — 线程崩溃后只有一条日志，没有重试

**更好的方案**：用 ASGI（Daphne/Uvicorn）+ `async def` 视图 + `asyncio.create_task`，或 Celery 异步任务。

**2. 没有 API 版本化**

所有路由直接挂在 `/api/` 下，没有 `/api/v1/` 前缀。这在 MVP 阶段可以接受，但在面试中会被问"如果要破坏性变更怎么办"。

**3. 手动分页不够健壮**

```python
# 多处视图中的模式
items = queryset[items_count:items_count + 20]
```

没有用 DRF 的 `PageNumberPagination`，也没有做 offset 越界保护。如果 `items_count` 传入负数会怎样？

**4. 没有输入验证层**

不使用 DRF Serializer 意味着所有输入校验都是手写的 `if not x: return 400`。好处是灵活，坏处是：
- 没有统一的校验错误格式
- 校验逻辑分散在各视图中
- 缺少字段类型转换（如字符串转 int）
- 无法自动生成 API 文档（OpenAPI/Swagger）

**5. 缺少缓存层**

首页角色列表、音色列表等高频读取数据没有任何缓存。虽然目前用户量不大，但面试官会问"如果首页 QPS 到 1000 怎么办"。

---

## 三、最能体现"高级后端工程师"能力的设计

### 1. JWT 双 Token + 订阅者队列刷新（api.js）

这是整个项目中**最接近高级工程师水平**的代码。不是简单的"过期了就跳登录页"，而是：
- 并发 401 排队：多个请求同时遇到 401，只发一次 refresh 请求
- 刷新成功后批量重试：所有排队的请求用新 token 重新执行
- SSE 流式连接独立处理：streamApi.js 有自己的重连逻辑
- refresh_token 存在 httpOnly cookie 中，JS 不可读（防 XSS）

这个设计如果能在面试中讲清楚，会是很大的加分项。

### 2. LangGraph State Graph 的工具调用路由

```python
# graph.py - 条件路由
def should_continue(state: AgentState):
    last_message = state["messages"][-1]
    if last_message.tool_calls:
        return "tools"
    return "end"
```

虽然是标准 LangGraph 用法，但配合 `search_knowledge_base` 工具（pgvector 原始 SQL 查询），体现了对 Agent 架构的理解。面试官可能追问："为什么不用 LangChain 的 AgentExecutor？State Graph 相比 Chain 有什么优势？"

### 3. 异步 TTS WebSocket 的双工流式

```python
# chat.py - TTS 发送/接收并发
async def run_tts_task(self, ...):
    async with websockets.connect(WSS_URL) as ws:
        await asyncio.gather(
            self.tts_sender(ws, app, inputs),
            self.tts_receiver(ws, mq),
        )
```

`asyncio.gather` 同时运行发送和接收协程，实现"LLM 一边生成文本一边合成语音"。理解这种并发模式需要较深的异步编程功底。

---

## 四、当前项目的不足与短板

### 架构层面

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| **WSGI + 同步视图 + 线程内嵌 asyncio** | 🔴 高 | 线程安全风险、连接泄漏风险、gunicorn worker 退出时后台线程被强杀 |
| **无异步任务队列** | 🔴 高 | Memory Agent 的摘要生成在请求线程中同步执行，阻塞用户响应。应该用 Celery/RQ 异步化 |
| **SECRET_KEY 硬编码** | 🔴 高 | `settings.py` 第 23 行，必须移到环境变量 |
| **前端 Image Croppie 代码重复** | 🟡 中 | `Photo.vue` 在两个地方几乎一模一样，应该抽 composable |
| **无 API 版本化** | 🟡 中 | 破坏性变更无法平滑迁移 |
| **.env 中的 API_KEY 未做安全隔离** | 🟡 中 | 生产环境 API Key 暴露在配置文件中，缺少密钥管理方案 |

### 测试层面

| 问题 | 说明 |
|------|------|
| **ASR 完全无测试** | WebSocket 协议、音频数据流、transcription 结果解析均未覆盖 |
| **TTS 无测试** | TTS WebSocket 连接在测试中被 mock 掉，实际音频流管道未验证 |
| **Homepage 无测试** | 搜索、分页、排序均未验证 |
| **Profile 更新无测试** | 头像裁剪、个人信息更新未覆盖 |
| **Voice 管理无测试** | 阿里云 API 调用的 create/list/delete voice 函数无测试 |
| **RAG 接入无测试** | CustomEmbeddings、insert_documents、pgvector 查询均无测试 |
| **无集成测试** | 所有测试都是单元测试，没有端到端的集成测试 |
| **48 个测试覆盖约 60% 端点** | 核心 CRUD 覆盖了，但语音、知识库、部署相关路径完全空白 |

### 可观测性

| 缺少项 | 影响 |
|------|------|
| **健康检查端点** | K8s/Docker 无法做 readiness/liveness probe |
| **请求耗时监控** | 无法感知慢查询和性能退化 |
| **错误率告警** | 生产环境异常只能靠日志被动发现 |
| **API 访问日志** | 没有结构化请求日志（middleware 层） |

### 运维与 DevOps

| 缺少项 |
|------|
| Docker 容器化 |
| docker-compose 一键部署 |
| CI/CD Pipeline |
| 数据库迁移自动化（目前靠手动 migrate） |
| 静态资源 CDN |

---

## 五、从面试官视角，项目还缺少哪些"真正有含金量"的内容

### P0 — 面试必问但当前缺失

1. **数据库事务与一致性保证**
   - 你的项目中，Message 保存和 Memory 更新在不同位置执行，没有事务保证。面试官会问："如果消息保存成功了但 Memory 更新失败了怎么办？"
   - **建议**：用 `transaction.atomic()` 或异步任务队列 + 最终一致性

2. **并发控制**
   - 同一用户同时发多条消息会怎样？当前没有幂等键、没有去重、没有请求排队。
   - **建议**：前端发送后禁用按钮直到收到响应；后端加幂等键

3. **系统容量评估**
   - "你的系统能支撑多少并发用户？瓶颈在哪里？" —— 你目前无法回答
   - **建议**：至少做一次简单的压测（locust/wrk），知道 DB 连接数、gunicorn worker 数、AI API 限流哪个是瓶颈

4. **AI 调用的可靠性保障**
   - LLM API 调用失败后的重试策略？TTS WebSocket 断开后的恢复？记忆摘要失败是否影响聊天？
   - **建议**：加 tenacity 重试装饰器（你已安装但未使用）、断路器等

### P1 — 体现技术深度的加分项

5. **流式响应的背压控制**
   - 当 AI 生成速度 > 网络传输速度时，会内存堆积。你用了 `queue.Queue()` 但没有大小限制
   - **建议**：`queue.Queue(maxsize=N)` + 生产者阻塞

6. **SSE 连接管理**
   - 用户关闭浏览器后，后台 TTS WebSocket 连接会怎样？没有超时、没有心跳
   - **建议**：SSE 加心跳（定期发送 comment 行 `: heartbeat\n\n`），WebSocket 加超时

7. **embedding 更新的增量策略**
   - 现在 `insert_documents` 是**全量删除 + 全量插入**。文档多了以后不可行
   - **建议**：增量更新策略（hash 对比 / 版本号）

8. **Voice 资源的 CRUD 管理**
   - 目前只有内置音色，自定义音色的 create/list/delete 函数写好了但前端没接
   - 这个功能本身不难，但涉及阿里云 API 调用、异步处理、资源状态管理，适合展示后端设计能力

---

## 六、哪些像 Demo，哪些像生产项目

### 像 Demo 的地方（面试减分项）

| 特征 | 说明 |
|------|------|
| **SECRET_KEY 硬编码** | 任何生产项目不会这样做 |
| **管理后台插入 RAG 文档** | 需要通过 Django shell 手动执行 `insert_documents.py`，真正的产品应该支持用户上传文档 |
| **手动部署、手动改代码切换环境** | `platform = 'cloud'` 要手动改 JS 文件，`DEBUG = True/False` 要手动改，没有环境变量驱动 |
| **无 API 版本化** | 所有 `/api/` 下直接挂路由 |
| **手动 offset 分页** | `items_count:items_count+20`，没有 PageNumberPagination、没有 Cursor 分页 |
| **无请求日志中间件** | 无法追踪每个请求的耗时、状态码、用户 ID |
| **前端无全局错误处理** | 没有统一 toast/notification 组件，错误散落在各组件内 |

### 像生产项目的地方（面试加分项）

| 特征 | 说明 |
|------|------|
| **JWT 双 Token + 订阅者队列刷新** | 这就是真实产品的做法 |
| **结构化日志 + 文件轮转** | `RotatingFileHandler` 10MB × 5 备份 |
| **Dual-DB 测试策略** | test 自动 SQLite，runtime 自动 PG，很实用 |
| **Nginx + Gunicorn + Unix Socket** | 标准 Python Web 应用部署范式 |
| **select_related 预加载** | 首页、好友列表、角色列表都做了 N+1 优化 |
| **safe property（photo_url）** | 防御性编程，防止 ImageField 无文件时崩溃 |
| **VAD 模型文件缓存** | Cache API 缓存 Silero VAD 的 ONNX/WASM 文件 |
| **processId 输出中断** | 用户快速连续发消息时忽略旧 SSE 响应 |

---

## 七、冲击 Java 后端 / AI 应用工程师岗位还需要哪些能力

### Java 后端方向

你需要补的不是代码能力，而是**大规模分布式系统的设计经验**。Java 岗位面的是这些：

| 能力 | 你的现状 | 建议 |
|------|---------|------|
| **微服务架构** | 单体 Django | 至少学 Spring Cloud / K8s 基础概念，能说清楚服务拆分的依据（限界上下文） |
| **消息队列** | 无 | 学 Kafka/RabbitMQ 基础，在你的项目中加一个 Celery + Redis 的异步任务 |
| **分布式缓存** | 无 | 学 Redis 基础数据结构 + 缓存策略（Cache-Aside、Write-Through） |
| **数据库分库分表** | 无 | 理解 ShardingSphere / Vitess 的基本概念 |
| **分布式事务** | 无 | 理解 Saga / TCC / 本地消息表的适用场景 |
| **高并发设计** | 无实践 | 至少做一次压测，知道 QPS、RT、P99 的含义 |
| **Java 生态** | 你说主语言 Java | 把本项目的核心逻辑（Agent、RAG、SSE）用 Spring Boot 重写一个简化版 |

**最关键的一条**：你的简历上会有一个 Python AI 项目，但投的是 Java 岗位。面试官第一反应会是"这人是不是转语言了？"你需要：
- 准备一个有说服力的理由（"我想通过 Python 快速验证 AI 产品思路，但我的工程能力底座是 Java"）
- 准备在面试中展示 Java 代码（至少准备一个 Spring Boot 的 side project）
- 能把本项目中的设计决策用 Java 生态术语描述（"如果用 Java 做，我会用 Spring WebFlux + Reactor 替代 Django + asyncio"）

### AI 应用工程师方向

这个方向和你的项目匹配度更高，但竞争也更激烈：

| 能力 | 你的现状 | 建议 |
|------|---------|------|
| **Prompt Engineering** | 已实践 | 你的 SystemPrompt 模型设计不错，但可以深入：Few-shot、Chain-of-Thought、结构化输出 |
| **RAG 进阶** | 基础 RAG | 学 Advanced RAG：HyDE、Re-ranking、Self-query、Multi-hop retrieval |
| **Agent 设计模式** | 基础 Agent | 学 Multi-Agent 协作、Human-in-the-loop、Agent 评估框架 |
| **模型服务化** | 无 | 了解 vLLM、Ollama、模型量化、推理优化 |
| **向量数据库** | pgvector 基础 | 了解 Milvus、Qdrant、Weaviate 的适用场景区别 |
| **AI 应用评估** | 无 | 了解 RAGAS、LangSmith、评估指标体系（忠实度、相关性、准确性） |

---

## 八、最值得扩展的功能方向（按求职价值排序）

### Tier 1 — 直接提升面试竞争力（建议优先做）

| 优先级 | 功能 | 求职价值 | 预计工时 | 理由 |
|--------|------|---------|---------|------|
| **1** | **Docker 化 + docker-compose 一键启动** | ⭐⭐⭐⭐⭐ | 2-3 天 | 这是"项目是否工程化"的第一门槛。一个 docker-compose up 就能跑起来的项目 vs 需要手动配环境的项目，面试官的感受天差地别 |
| **2** | **Celery + Redis 异步任务（Memory Agent）** | ⭐⭐⭐⭐⭐ | 2-3 天 | 分布式任务队列是后端面试必问。你现在 Memory Agent 同步阻塞，改成异步后就是"消息队列在 AI 场景的应用" |
| **3** | **API 测试覆盖率提升到 80%+** | ⭐⭐⭐⭐⭐ | 3-5 天 | 补充 ASR、TTS、Homepage、RAG 测试。面试官判断代码质量的第一指标 |
| **4** | **用户上传文档构建 RAG** | ⭐⭐⭐⭐ | 3-5 天 | 完整的"用户上传 → 文本提取 → 分块 → embedding → 存储 → 检索"链路，AI 应用工程师的核心能力展示 |
| **5** | **用户自定义角色音色** | ⭐⭐⭐⭐ | 2-3 天 | 涉及阿里云 API 调用、文件上传、异步处理、状态管理，展示后端设计能力 |

### Tier 2 — 加分项（有额外时间就做）

| 优先级 | 功能 | 求职价值 | 预计工时 |
|--------|------|---------|---------|
| **6** | **ASGI 迁移（Daphne/Uvicorn）+ async 视图** | ⭐⭐⭐ | 3-5 天 |
| **7** | **Redis 缓存首页角色列表 + 音色列表** | ⭐⭐⭐ | 1-2 天 |
| **8** | **API 文档（drf-spectacular / Swagger）** | ⭐⭐⭐ | 1 天 |
| **9** | **健康检查 + Prometheus metrics 端点** | ⭐⭐⭐ | 1-2 天 |
| **10** | **速率限制（django-ratelimit）** | ⭐⭐ | 1 天 |
| **11** | **GitHub CI/CD（pytest + lint）** | ⭐⭐ | 1-2 天 |
| **12** | **多轮对话评估（RAGAS / 自定义 eval）** | ⭐⭐ | 2-3 天 |

---

## 九、高级工程化路线图（4 阶段）

### 阶段 0：安全止血（1-2 天）

```
[ ] SECRET_KEY 移到 .env
[ ] 检查 .env 是否在 .gitignore
[ ] 添加健康检查端点 GET /api/health/
[ ] settings.py 中 DEBUG 从环境变量读取，默认 False
```

### 阶段 1：生产基础设施（1-2 周）

```
[ ] Dockerfile（backend）+ Dockerfile（frontend build stage）
[ ] docker-compose.yml（backend + frontend + PostgreSQL + Redis）
[ ] Celery + Redis 异步任务（Memory Agent 异步化）
[ ] Redis 缓存首页高频数据
[ ] Gunicorn 配置优化（worker 数、timeout、keepalive）
[ ] Nginx 反向代理配置优化（gzip、静态资源缓存策略）
[ ] API 速率限制（登录接口 5次/分钟，聊天接口 20次/分钟）
```

### 阶段 2：架构升级 + 测试完善（2-3 周）

```
[ ] 补充测试至 80%+ 覆盖率（ASR、TTS、Homepage、RAG、Voice）
[ ] ASGI 迁移评估（是否需要 async 视图）
[ ] 用户上传文档 RAG（文档上传 → 文本提取 → 分块 → embedding → 检索）
[ ] 用户自定义音色（录音上传 → 阿里云 API 注册 → 音色绑定到角色）
[ ] API 版本化（/api/v1/）
[ ] 统一错误响应格式
[ ] drf-spectacular API 文档
```

### 阶段 3：深度能力展示（3-4 周）

```
[ ] SSE 背压控制（queue.Queue maxsize）
[ ] AI 调用重试策略（tenacity exponential backoff）
[ ] LangGraph checkpointer 持久化（对话状态可恢复）
[ ] 多轮对话评估框架
[ ] 简单压测报告（Locust，记录 QPS/P99）
[ ] GitHub Actions CI/CD
[ ] Chat Agent 支持多工具并联调用
[ ] Multi-Agent 协作（例如：规划 Agent + 执行 Agent）
```

---

## 十、简历包装建议

### 项目名称

```
AI Friends — 基于多 Agent 架构的实时语音交互 AI 应用
```

### 一句话描述（放简历标题下方）

> 全栈开发的实时语音 AI 聊天应用。采用 LangGraph 双 Agent 架构（对话 + 记忆），集成全双工语音管道（VAD/ASR/LLM/TTS），支持 pgvector 向量检索增强生成（RAG），使用 JWT 双 Token 认证方案。

### 简历 STAR 要点（按面试追问概率排序）

**1. Agent 架构设计**
- 基于 LangGraph State Graph 构建 Chat Agent，实现 LLM 工具调用路由（时间查询 + 知识库检索）
- 独立 Memory Agent（每 10 轮对话自动触发摘要），使用独立模型降低推理成本

**2. 实时语音管道**
- 设计并实现全双工语音流式管道：浏览器端 VAD（Silero VAD ONNX）→ WebSocket ASR（DashScope）→ SSE 流式文本 → WebSocket TTS 合成 → MSE 音频播放
- 使用 asyncio.gather 实现 TTS 发送/接收的并发流式处理，降低首音延迟

**3. JWT 认证与安全**
- 实现 access_token（2h）+ httpOnly refresh_token（7d）的双 Token 认证方案
- 前端实现订阅者队列模式处理并发 401 无感刷新，SSE 流式连接独立的 Token 过期恢复机制

**4. RAG 知识库**
- 基于 pgvector 实现向量检索增强生成（1024 维 DashScope text-embedding-v4）
- 自实现 Markdown 分块策略（MarkdownHeaderTextSplitter + RecursiveCharacterTextSplitter）

**5. 工程实践**
- pytest 48 个测试用例，Dual-DB 策略（测试 SQLite / 生产 PostgreSQL 17）
- Django + DRF 纯 APIView 设计，每个端点单一职责，结构化日志 + RotatingFileHandler
- Nginx + Gunicorn + Unix Socket 生产部署，HTTPS 自签名证书
- select_related 预加载 + 复合索引优化 N+1 查询问题

### 面试话术模板

**被问"这个项目有什么难点"时的回答框架：**

> "最难的其实是 SSE 流式响应 + 实时 TTS 的衔接。因为聊天回复是 token 级别的流式输出，TTS 同步需要把文本流实时转成音频流。我的做法是用 asyncio.gather 同时跑两个协程——一个从 LangGraph stream 读文本推送到 TTS WebSocket，另一个从 TTS WebSocket 读音频帧 base64 编码后放进 queue.Queue，主线程从 queue 取音频和文本拼成 SSE 事件发给前端。这里面有几个细节：TTS WebSocket 文本发送完了要发 finish-task 信号，音频帧的边界要对齐，前端 MediaSource 的 SourceBuffer 要处理 append 队列..."

**被问"为什么不用 X 技术"时的回答要点：**

- 为什么不用 LangChain Chain？→ State Graph 更灵活，条件路由天然支持工具调用循环
- 为什么不用 WebSocket 双向？→ 聊天场景是请求-流式响应，SSE 更简单，前端发消息用 POST 就够了
- 为什么不用 DRF Serializer？→ AI 应用的输入输出结构多变，手写校验更灵活

---

## 十一、面试官视角的总评

### 最能加分的地方

1. **双 Agent + 全双工语音**。这是真正的 AI 应用工程能力，不是调 API demo。面试官会觉得"这人真的把 AI 用起来了"。
2. **JWT 订阅者队列刷新**。这是真实产品才会遇到的问题，你的解决方案体现了对并发和边界条件的思考。
3. **从裸 except 到全量日志的工程化进化**。如果你能讲清楚"最早写的很随意，后来怎么一步步加上日志、测试、PG 迁移"，这本身就是很好的工程成长故事。

### 最容易被质疑的地方

1. **"你是 Java 后端，为什么用 Python/Django 做项目？"** — 如果投 Java 岗位，这个问题必须准备好答案。
2. **"你的 AI 能力都是调阿里云 API，自己做了什么？"** — 需要强调 Agent 架构设计、流式管道编排、工程化落地，而不是模型训练。
3. **"项目上线了没有？有用户吗？"** — 如果没有，就坦率说"这是我个人的 AI 应用实践项目，目的是验证 Agent 架构和实时语音交互的技术可行性"。不要吹用户量。
4. **"为什么 SECRET_KEY 还硬编码？"** — 这是低级错误，尽快修掉。

### 面试官会追问的技术问题（预测）

| 问题 | 考察点 |
|------|--------|
| "LangGraph State Graph 和 LangChain Chain 的区别？你为什么选前者？" | Agent 架构理解 |
| "SSE 和 WebSocket 的区别？聊天为什么用 SSE 而不是 WebSocket？" | 协议选型能力 |
| "你的 Memory Agent 触发机制是什么？如果 10 条消息中有系统消息怎么办？" | 边界条件思考 |
| "pgvector 的余弦距离和欧氏距离哪个更适合你的场景？为什么？" | 向量检索理解 |
| "你的 JWT refresh token 存在 cookie 里，如果被 XSS 攻击了怎么处理？" | 安全意识 |
| "如果聊天 QPS 到 100，你的系统瓶颈在哪里？怎么扩容？" | 系统设计能力 |
| "LangGraph 的工具调用失败率有统计吗？失败后怎么兜底？" | AI 工程可靠性 |
| "asyncio.run() 在 Django 同步视图中运行安全吗？为什么？" | 异步编程理解 |
| "你的 embedding 模型 1024 维，chunk 大小 500 字符，这两个参数怎么选的？" | RAG 调参经验 |
| "如果把 Django 换成 Spring Boot，你会怎么设计 Agent 的流式响应？" | 跨语言迁移能力 |

### 技术深度评分

| 维度 | 评分 | 说明 |
|------|------|------|
| **后端基础（CRUD/认证/部署）** | 7/10 | 功能齐全，但缺少 API 版本化、统一错误格式、请求日志中间件 |
| **数据库设计** | 7/10 | 索引合理，N+1 优化到位，但缺少事务管理、连接池配置 |
| **异步编程** | 6/10 | 理解 asyncio 基本用法，但 WSGI + 线程 + asyncio 的组合有风险 |
| **AI Agent 设计** | 7/10 | 双 Agent 拆分合理，但缺少评估、重试、checkpointer 持久化 |
| **RAG 实现** | 6/10 | 基础 RAG 链路完整，但全量重建、无增量更新、无 reranking |
| **流式处理** | 7/10 | SSE + TTS 双工流式是亮点，但缺少背压控制和断线恢复 |
| **工程化** | 6/10 | 日志和测试从无到有进步大，但缺 Docker/CI/CD/可观测性 |
| **安全** | 5/10 | JWT 方案不错，但 SECRET_KEY 硬编码、无速率限制、无 CSP |

### 社招竞争力评估

| 目标岗位 | 竞争力 | 说明 |
|---------|--------|------|
| **Java 后端（P6/高级）** | ⚠️ 偏弱 | 项目栈不匹配，缺少分布式系统经验。建议补一个 Spring Boot side project 或把核心逻辑用 Java 重写 |
| **Java 后端（P5/中级）** | ✅ 可竞争 | 如果面试能讲清楚项目设计，Java 基础扎实，可以到 P5 |
| **AI 应用工程师（全栈）** | ✅ 有竞争力 | 项目匹配度高，双 Agent + 语音管道是差异化亮点。补上 Docker + 测试覆盖率 + 异步任务后竞争力明显提升 |
| **Python 后端（AI 方向）** | ✅ 有竞争力 | 项目直接对口，但需要补异步编程（FastAPI/async Django）和模型服务化知识 |
| **创业公司 / AI Startup** | ✅ 竞争力强 | 创业公司看重"能独立把想法跑通"的能力，这个项目刚好展示这一点 |

### GitHub / 开源社区吸引力

**当前吸引力：⭐⭐（5 分制）**

理由：
- 功能完整度不错，但缺少 README 英文版、架构图、Quick Start（docker-compose up 一键启动）
- 没有 CONTRIBUTING.md、LICENSE、Issue Template
- 代码注释较少，外部贡献者难以理解
- 阿里云 API 依赖使得其他开发者无法直接运行

**要做到有吸引力（⭐⭐⭐⭐），需要：**
1. docker-compose 一键启动（本地 LLM 回退方案，如 Ollama）
2. 英文 README + 架构图（mermaid/ascii）
3. 去掉云服务硬依赖（至少支持本地模型）
4. 录制 Demo 视频

### 是否达到"AI 岗位加分项目"标准

**结论：达到了 70%，还差关键一步。**

加分项目的标准是：面试官看完能问出有深度的问题，而你能接住。

你的项目可以引发这些问题（好现象）：
- "LangGraph 的工具调用怎么做的？"
- "流式 TTS 怎么和 SSE 衔接的？"

你目前可能接不住的问题：
- "为什么在生产环境硬编码 SECRET_KEY？"
- "你的系统做了压测吗？QPS 多少？"
- "Memory Agent 失败了怎么不影响聊天？"

**修掉这些硬伤，把 Docker + 测试覆盖率 + 异步任务补上，这个项目就是标准的"AI 岗位加分项目"。**

---

## 十二、总结

这个项目经过前两轮 Review 后的改进（消除裸 except、添加日志、补测试、PG 迁移）方向完全正确，工程化水平有明显提升。

**当前最大的三个问题：**
1. SECRET_KEY 硬编码（一票否决级）
2. WSGI + 线程 + asyncio 的架构风险（技术债）
3. 缺少 Docker/CI/CD/可观测性（工程化完整度不够）

**性价比最高的三个改进（投入少、面试收益大）：**
1. SECRET_KEY 移到环境变量（10 分钟）
2. Docker + docker-compose 一键启动（2 天）
3. Celery + Redis 异步任务替换 Memory Agent 同步调用（2 天）

**一句话评价：**
> 这是一个"有亮点但未完成工程化闭环"的 AI 应用项目。Agent 架构和语音管道的设计体现了一定的技术深度，JWT 认证方案接近生产标准，但基础设施层的缺失（Docker、CI/CD、异步任务、安全配置）让它在面试中容易被追问"为什么没做完"。补齐基础设施后，这个项目对 AI 应用工程师岗位有较强的竞争力。

---

*Review Date: 2026-05-22*
*评估标准：头部互联网公司 P6（高级工程师）/ AI 应用工程师（社招）*
