## 服务器部署（Docker Compose）

`docker compose up -d` 一键启动全栈：PostgreSQL 17 + Redis 7 + Django/gunicorn + Celery Worker(+Beat) + Nginx。

部署跨两台机器：

- **本地构建机**：构建前端（`npm run build`）+ 收集静态（`collectstatic`）。云服务器内存有限，`npm install` 易 OOM，故前端在本地构建后把产物传上去。
- **云服务器**：`git clone` 源码 → 现场 `docker compose build` 构建 django/celery 镜像 → 运行 5 容器。

### 前置依赖

| 机器 | 依赖 | 用途 |
|------|------|------|
| 本地构建机 | Node.js + npm；Python 3.12 + 项目依赖 | `npm run build` + `collectstatic` |
| 云服务器 | Docker + Docker Compose v2；git；openssl | clone 源码、构建镜像、跑容器、签证书 |

#### 安装 Docker（云服务器，首次）

```bash
sudo apt-get update && sudo apt-get install -y docker.io docker-compose-v2 git
sudo systemctl enable --now docker
sudo usermod -aG docker $USER   # 重新登录生效
```

### 首次部署

#### 1.（云服务器）克隆源码

```bash
ssh gqyin@115.190.245.146
git clone git@github.com:5ME/AiFriends.git ~/ai-friends && cd ~/ai-friends
git checkout feature/gqyin/p1-d1-docker-compose   # 合并到 master 后用 master
```

> 私有仓库需服务器具备 git 访问权限（deploy key 或 token）。

#### 2.（云服务器）环境变量 + SSL 证书

```bash
cp .env.example .env
# 编辑 .env，填真实值：
#   - API_KEY / API_BASE / WSS_URL / VOICE_URL（DashScope 密钥）
#   - DJANGO_SECRET_KEY（随机长串）
#   - PG_PASSWORD（见下方"复用现有 PG 卷"说明）
#   - DJANGO_ALLOWED_HOSTS=115.190.245.146
#   - DJANGO_CORS_ORIGINS=https://115.190.245.146
#   - DJANGO_MEDIA_URL=https://115.190.245.146/media/
# 已预填无需改：PG_HOST=postgres、CELERY_BROKER_URL/REDIS_URL 用 redis 服务名、DJANGO_DEBUG=False

mkdir -p ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout ssl/aifriends-selfsigned.key \
  -out ssl/aifriends-selfsigned.crt -subj '/CN=115.190.245.146'
```

> **⚠️ 复用现有 PG 卷**：服务器若已用旧 docker compose 跑过 PostgreSQL，`postgres-data` 卷已存在（含生产数据）。新 compose 复用该卷——`POSTGRES_PASSWORD` 对**已存在的卷无效**，`aifriends` 用户保持原密码。所以 `.env` 的 `PG_PASSWORD` 必须 = 现有 `aifriends` 用户的密码（即你当前服务器 `backend/.env` 里那个），否则 Django 连不上。

#### 3.（云服务器）停掉裸机服务

Docker 的 nginx 要占 80/443，与裸机 nginx 冲突；裸机 gunicorn/celery 由容器接管。先停：

```bash
sudo systemctl stop nginx
# 停裸机 gunicorn / celery（按你的进程管理方式，如 systemctl stop celery-worker）
```

#### 4.（本地构建机）构建前端 + 收集静态 → 传到服务器

```bash
cd frontend && npm install && npm run build && cd ..
cd backend && python manage.py collectstatic --noinput && cd ..
scp -r backend/staticfiles gqyin@115.190.245.146:ai-friends/backend/
```

> `staticfiles/` 是 gitignore 的构建产物，clone/pull 不会带它，需本地构建后传上去。`git pull` 也不会动它，所以后续更新时它仍在。

#### 5.（云服务器）迁移 + 启动

```bash
cd ~/ai-friends
docker compose run --rm django python manage.py migrate
# docker compose run --rm django python manage.py createsuperuser   # 可选
docker compose up -d --build
docker compose ps   # 5 容器全 running / healthy
```

浏览器访问 `https://115.190.245.146`（自签证书 → 浏览器警告，点继续访问）。

### 更新部署

```bash
# 本地构建机：重建前端 + 收集静态 + 传产物
cd frontend && npm run build && cd ..
cd backend && python manage.py collectstatic --noinput && cd ..
scp -r backend/staticfiles gqyin@115.190.245.146:ai-friends/backend/

# 云服务器：拉代码 + 迁移 + 重建重启
cd ~/ai-friends && git pull
docker compose run --rm django python manage.py migrate
docker compose up -d --build
```

### 回滚到裸机部署

```bash
cd ~/ai-friends
docker compose down            # 停 Docker 栈（PG/Redis 数据卷保留）
sudo systemctl start nginx     # 恢复裸机 nginx
# 恢复裸机 gunicorn / celery
```

### 常用运维命令

```bash
docker compose ps                       # 容器状态
docker compose logs -f                  # 所有日志
docker compose logs -f django           # 单个服务日志
docker compose restart django           # 重启单个服务
docker compose down                     # 停止所有容器
docker compose down -v                  # 停止并删除数据卷（⚠️ 数据库数据会丢失）
```

### 容器拓扑

```
Nginx (:443/:80)  →  Django/gunicorn (:8000)  →  PostgreSQL
                                              →  Redis
                  Celery Worker(+Beat)        →  PostgreSQL
                                              →  Redis
```

| 容器 | 镜像 | 对外端口 |
|------|------|---------|
| ai-friends-nginx | nginx:1.27-alpine | 443, 80 |
| ai-friends-web | 本地构建（gunicorn） | — |
| ai-friends-celery | 本地构建（worker + beat） | — |
| ai-friends-db | pgvector/pgvector:pg17 | 127.0.0.1:55432（仅本机） |
| ai-friends-redis | redis:7-alpine | 127.0.0.1:6379（仅本机） |

### 注意事项

- **前端在本地构建**：云服务器内存有限，`npm install` 易 OOM；前端产物（`backend/staticfiles/`）本地 build 后 scp，不在服务器上 build
- **复用现有 PG 卷**：`.env` 的 `PG_PASSWORD` 须 = 现有 `aifriends` 用户密码（见上方说明）
- **数据持久化**：`postgres-data` / `redis-data` 为 named volume；`media` / `logs` / `staticfiles` 为 bind mount
- **PG/Redis 端口**：仅绑定 `127.0.0.1`（外网由安全组阻断，宿主机可 `psql -h 127.0.0.1 -p 55432` 调试）
- **自签证书**：浏览器提示不安全，属预期（IP 部署无域名证书）
- **首次镜像构建**：django/celery 镜像在服务器上 `docker compose build` 现场构建（依赖全是 wheel，不吃内存，约几分钟）
